import 'package:flutter_test/flutter_test.dart';


main() {
  testWidgets('ContatoPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ContatoPage(title: 'Contato')));
    //  final titleFinder = find.text('Contato');
    //  expect(titleFinder, findsOneWidget);
  });
}
