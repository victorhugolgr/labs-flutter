import 'package:flutter_test/flutter_test.dart';

main() {
  testWidgets('ContatoEditPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ContatoEditPage(title: 'ContatoEdit')));
    //  final titleFinder = find.text('ContatoEdit');
    //  expect(titleFinder, findsOneWidget);
  });
}
