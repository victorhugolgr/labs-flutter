import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';


main() {
  testWidgets('ContatoDetailPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ContatoDetailPage(title: 'ContatoDetail')));
    //  final titleFinder = find.text('ContatoDetail');
    //  expect(titleFinder, findsOneWidget);
  });
}
